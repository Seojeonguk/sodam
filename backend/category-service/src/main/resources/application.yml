server:
  port: 10004

spring:
  application:
    name: category-service
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3310/sodam
    username: sodam
    password: 1s2o3d4a5m
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
    open-in-view: false
  cloud:
    discovery:
      enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    com.example.categoryservice.infrastructure.UserServiceClient: DEBUG
    feign: DEBUG
    org:
      hibernate:
        orm:
          jdbc:
            bind: TRACE